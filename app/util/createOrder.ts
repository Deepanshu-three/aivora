import axios from "axios";
import { toast } from "sonner";
import { useRouter } from "next/navigation";

export async function createOrder(addressId: string, router?: ReturnType<typeof useRouter>) {
 
}
